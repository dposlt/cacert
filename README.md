### AUTOMATIC CREATE CERTIFIKATE ###

- PYTHON 3.6
- PIP INSTALL COLORMA
- OPENSSL
- edit path needed

-- NOTICE --
- CNF defined DNS records for the certificate
- cert.py is head program for create certificate
- config.ini -> define path to the certificate